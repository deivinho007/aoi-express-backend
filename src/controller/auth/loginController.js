export const loginController = (req,res) => {

    // receber o email e a senha do corpo da requisição

    //comparar se o email e senha batem com oque esta no banco de dados

    //se bater, gerar um token JWT

    // enviar o token para cliente 
    return 
}